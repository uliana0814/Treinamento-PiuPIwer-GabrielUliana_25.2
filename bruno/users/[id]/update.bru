meta {
  name: update
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/{{folder}}/{{userId}}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "MENGAO!!!! O MAIOR!!"
    // para update de email e senha, o betterAuth
    // não permite (as of now) mudar de 3°s
    // a separação em vários arquivos não é 100%
    // necessária, mas facilita
  }
}
